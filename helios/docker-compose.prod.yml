services:
  helios:
    labels: 
      - "traefik.http.routers.helios.rule=Host(`helios.rose-croix-d-or.org`)"
    networks:
      rconetwork:
    secrets:
      - mysql_password
      - jwt_secret
networks:
  rconetwork:
    external: true
secrets:
  mysql_password:
    file: .secret.helios.prod.mysql.password
  jwt_secret:
    file: .secret.helios.prod.jwt.secret