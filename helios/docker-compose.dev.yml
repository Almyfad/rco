services:
  helios:
    container_name: helios-dev
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    labels:
      traefik.http.routers.helios.rule: Host(`helios-dev.rose-croix-d-or.org`)
    networks:
      rconetwork:
    secrets:
      - mysql_password
      - jwt_secret
networks:
  rconetwork:
    external: true
secrets:
  mysql_password:
    file: $HOME/helios/.secret.helios.dev.mysql.password
  jwt_secret:
    file: $HOME/helios/.secret.helios.dev.jwt.secret